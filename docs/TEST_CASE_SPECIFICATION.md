# 테스트 케이스 명세서 (Test Case Specification)

> 출처: `docs/RETROSPECTIVE_TEST_ANALYSIS.md` — fix 커밋 20개 기반 사후 분석
> 작성 관점: QA 엔지니어
> 작성일: 2026-03-17

---

## 카테고리 1 — 채팅 메시지 상태 관리

| 항목 | 내용 |
|------|------|
| **ID** | TC-001 |
| **시나리오** | 이미지만 첨부한 메시지가 AI 응답 후에도 대화창에 유지되는지 확인 |
| **사전 조건** | 사용자가 로그인 상태이며 채팅 화면이 열려 있음. 텍스트 없이 이미지 파일만 첨부 가능한 상태 |
| **테스트 스텝** | 1. 텍스트 입력 없이 이미지 파일만 첨부한다<br>2. 전송 버튼을 클릭한다<br>3. AI 응답이 완료될 때까지 대기한다<br>4. 대화창에서 전송한 이미지 메시지를 확인한다 |
| **기대 결과** | AI 응답 완료 후에도 사용자가 첨부한 이미지 메시지가 대화창에 그대로 표시된다 |
| **실제 결과** | _(미실행 — 사전 테스트 명세)_ |

| 항목 | 내용 |
|------|------|
| **ID** | TC-002 |
| **시나리오** | 선택지 클릭 시 내부 ID가 아닌 라벨 텍스트가 DB에 저장되는지 확인 |
| **사전 조건** | 채팅 흐름에서 카테고리 선택 단계에 도달한 상태. "패션" 등 선택지가 표시됨 |
| **테스트 스텝** | 1. "패션" 선택지 버튼을 클릭한다<br>2. 페이지를 새로고침한다<br>3. 해당 사용자 메시지의 표시 텍스트를 확인한다 |
| **기대 결과** | 저장된 메시지에 `option-1`, `option-2` 등 내부 ID가 아닌 "패션" 라벨이 표시된다 |
| **실제 결과** | _(미실행 — 사전 테스트 명세)_ |

| 항목 | 내용 |
|------|------|
| **ID** | TC-003 |
| **시나리오** | 이미지 미첨부 상태에서 "완료" 입력 시 이미지 요청 메시지가 출력되는지 확인 |
| **사전 조건** | 채팅 흐름의 정보 수집 단계에서 제품명·카테고리를 입력 완료한 상태. 이미지는 업로드하지 않음 |
| **테스트 스텝** | 1. 제품명: "나이키 운동화", 카테고리: "패션"을 입력한다<br>2. "완료"를 입력하여 전송한다<br>3. AI 응답 메시지를 확인한다 |
| **기대 결과** | 기획 단계 메시지가 아닌 이미지 업로드를 요청하는 메시지가 출력된다 |
| **실제 결과** | _(미실행 — 사전 테스트 명세)_ |

| 항목 | 내용 |
|------|------|
| **ID** | TC-004 |
| **시나리오** | "브랜드 없이 진행" 선택 후 무한 루프 없이 다음 단계로 진행되는지 확인 |
| **사전 조건** | 채팅 흐름에서 브랜드 선택 단계에 도달한 상태. "브랜드 없이 진행" 선택지가 표시됨 |
| **테스트 스텝** | 1. "브랜드 없이 진행" 선택지를 클릭한다<br>2. 2초 대기 후 화면을 확인한다<br>3. "브랜드 없이 진행" 선택지가 다시 표시되는지 확인한다 |
| **기대 결과** | "브랜드 없이 진행" 선택지가 사라지고 다음 단계로 정상 진행된다 (루프 없음) |
| **실제 결과** | _(미실행 — 사전 테스트 명세)_ |

---

## 카테고리 2 — 입력 유효성 검사 & API 계약

| 항목 | 내용 |
|------|------|
| **ID** | TC-005 |
| **시나리오** | `brandProfileId: null`로 프로젝트 생성 시 500 오류가 아닌 201이 반환되는지 확인 |
| **사전 조건** | 인증된 사용자 세션 보유. 브랜드 프로필을 선택하지 않은 상태 |
| **테스트 스텝** | 1. `POST /api/projects`에 `{ brandProfileId: null, workspaceId: null, name: "테스트" }`를 전송한다<br>2. HTTP 응답 상태 코드를 확인한다<br>3. 응답 본문의 오류 메시지를 확인한다 |
| **기대 결과** | HTTP 201이 반환되며 `"expected string"` 등 Zod 유효성 오류가 발생하지 않는다 |
| **실제 결과** | _(미실행 — 사전 테스트 명세)_ |

| 항목 | 내용 |
|------|------|
| **ID** | TC-006 |
| **시나리오** | 브랜드가 0개인 상태에서 프로젝트 생성 페이지에 "브랜드 없이 진행" 옵션이 표시되는지 확인 |
| **사전 조건** | 사용자 계정에 등록된 브랜드 프로필이 없음. 프로젝트 생성 페이지 접근 가능 |
| **테스트 스텝** | 1. `/api/brands` 응답을 `{ data: [] }`로 모킹한다<br>2. `/dashboard/projects/new` 페이지에 접근한다<br>3. "브랜드 없이 진행" 옵션의 표시 여부를 확인한다 |
| **기대 결과** | 브랜드 목록이 비어 있어도 "브랜드 없이 진행" 옵션이 UI에 표시된다 |
| **실제 결과** | _(미실행 — 사전 테스트 명세)_ |

| 항목 | 내용 |
|------|------|
| **ID** | TC-007 |
| **시나리오** | 브랜드 상세 페이지 접근 시 `.map is not a function` 런타임 오류가 발생하지 않는지 확인 |
| **사전 조건** | 등록된 브랜드 프로필이 하나 이상 존재. 해당 브랜드에 연결된 프로젝트가 없거나 빈 배열로 반환됨 |
| **테스트 스텝** | 1. 브라우저 콘솔 에러 리스너를 등록한다<br>2. `/dashboard/brands/{id}` 페이지에 접근한다<br>3. 네트워크 유휴 상태까지 대기한다<br>4. 콘솔 에러 중 `.map` 관련 오류를 확인한다 |
| **기대 결과** | 페이지가 오류 없이 정상 렌더링되며 콘솔에 `.map is not a function` 오류가 없다 |
| **실제 결과** | _(미실행 — 사전 테스트 명세)_ |

| 항목 | 내용 |
|------|------|
| **ID** | TC-008 |
| **시나리오** | 이메일 로그인 세션으로 프로젝트 생성 시 FK 제약 오류 없이 200이 반환되는지 확인 |
| **사전 조건** | 이메일/비밀번호로 로그인한 세션 쿠키 보유. 해당 세션의 `sub` 값이 DB `users.id`와 동일 |
| **테스트 스텝** | 1. 이메일/비밀번호로 로그인하여 세션 쿠키를 획득한다<br>2. 해당 쿠키로 `POST /api/projects`에 `{ name: "테스트 프로젝트" }`를 전송한다<br>3. HTTP 응답 상태 코드와 본문을 확인한다 |
| **기대 결과** | HTTP 200(또는 201)이 반환되며 응답 본문에 `"Foreign key"` 관련 오류 문자열이 없다 |
| **실제 결과** | _(미실행 — 사전 테스트 명세)_ |

---

## 카테고리 3 — UI 상태 & 네비게이션

| 항목 | 내용 |
|------|------|
| **ID** | TC-009 |
| **시나리오** | 각 대시보드 경로에서 사이드바의 정확한 메뉴 하나만 활성화되는지 확인 |
| **사전 조건** | 사용자가 로그인 상태. 사이드바가 있는 대시보드 레이아웃 접근 가능 |
| **테스트 스텝** | 1. `/dashboard` 접근 후 활성화된 메뉴 수와 텍스트를 확인한다<br>2. `/dashboard/chat` 접근 후 동일하게 확인한다<br>3. `/dashboard/projects` 접근 후 동일하게 확인한다 |
| **기대 결과** | 각 경로에서 `data-active="true"` 요소가 정확히 1개이며, `/dashboard`→"홈", `/dashboard/chat`→"채팅", `/dashboard/projects`→"프로젝트"가 활성화된다 |
| **실제 결과** | _(미실행 — 사전 테스트 명세)_ |

| 항목 | 내용 |
|------|------|
| **ID** | TC-010 |
| **시나리오** | 이미지 업로드 오류 시 브라우저 `alert()` 대신 토스트 메시지가 표시되는지 확인 |
| **사전 조건** | 이미지 업로드 UI가 있는 페이지 접근. 허용 용량을 초과하거나 지원하지 않는 형식의 파일 준비 |
| **테스트 스텝** | 1. `dialog` 이벤트 리스너를 등록하여 `alert()` 감지 설정을 한다<br>2. 허용 용량을 초과한 파일을 이미지 업로드 필드에 첨부한다<br>3. 토스트 메시지 표시 여부를 확인한다<br>4. `alert()` 다이얼로그 발생 여부를 확인한다 |
| **기대 결과** | 토스트 메시지가 표시되고 `alert()` 다이얼로그는 발생하지 않는다 |
| **실제 결과** | _(미실행 — 사전 테스트 명세)_ |

| 항목 | 내용 |
|------|------|
| **ID** | TC-011 |
| **시나리오** | 이미지 생성·오버레이·기본 탭 헤더의 높이가 모두 동일한지 확인 |
| **사전 조건** | 에디터 페이지에서 이미지 생성 탭, 오버레이 탭, 기본 탭이 모두 표시된 상태 |
| **테스트 스텝** | 1. `image-gen`, `overlay`, `basic` 각 탭의 헤더 요소를 선택한다<br>2. `getBoundingClientRect().height`로 각 높이값을 측정한다<br>3. 세 값이 모두 동일한지 비교한다 |
| **기대 결과** | 세 탭 헤더의 높이값이 모두 동일하다 |
| **실제 결과** | _(미실행 — 사전 테스트 명세)_ |

---

## 카테고리 4 — AI 생성 & 프롬프트 출력

| 항목 | 내용 |
|------|------|
| **ID** | TC-012 |
| **시나리오** | 섹션 재생성 후 훅 메시지와 나머지 섹션이 유지되는지 확인 |
| **사전 조건** | AI 생성이 완료된 상세 페이지 프로젝트가 열려 있음. 훅 메시지와 최소 2개의 섹션이 표시된 상태 |
| **테스트 스텝** | 1. 현재 훅 메시지 텍스트를 기록한다<br>2. 첫 번째 섹션의 재생성 버튼을 클릭한다<br>3. 재생성이 완료될 때까지 대기한다<br>4. 훅 메시지 텍스트를 재확인한다 |
| **기대 결과** | 재생성 완료 후 훅 메시지가 재생성 전과 동일하게 유지된다 |
| **실제 결과** | _(미실행 — 사전 테스트 명세)_ |

| 항목 | 내용 |
|------|------|
| **ID** | TC-013 |
| **시나리오** | 초기 생성과 재생성에서 최종 프롬프트 구성 요소가 동일한지 확인 |
| **사전 조건** | `/api/generate/section` 엔드포인트 접근 가능. 동일한 페이로드로 두 번 요청 가능 |
| **테스트 스텝** | 1. 동일한 페이로드로 `POST /api/generate/section`을 처음 호출한다<br>2. 동일한 페이로드로 재생성 요청을 한 번 더 호출한다<br>3. 두 응답의 `revisedPrompt` 구성 요소를 비교한다 |
| **기대 결과** | 재생성 응답의 `revisedPrompt`가 초기 생성에 사용된 `overlayTextPrompt` 등 구성 요소를 동일하게 포함한다 |
| **실제 결과** | _(미실행 — 사전 테스트 명세)_ |

| 항목 | 내용 |
|------|------|
| **ID** | TC-014 |
| **시나리오** | `fontSize` 경계값(12, 80)과 범위 외 값(11, 81)에서 API가 올바르게 처리되는지 확인 |
| **사전 조건** | `/api/generate/section` 엔드포인트 접근 가능. `fontSize` 유효 범위는 12~80 |
| **테스트 스텝** | 1. `fontSize: 11`로 요청하여 응답 상태 코드를 확인한다 (범위 외 하한)<br>2. `fontSize: 12`로 요청하여 응답 상태 코드를 확인한다 (유효 하한)<br>3. `fontSize: 80`으로 요청하여 응답 상태 코드를 확인한다 (유효 상한)<br>4. `fontSize: 81`로 요청하여 응답 상태 코드를 확인한다 (범위 외 상한) |
| **기대 결과** | `11`, `81`은 HTTP 400을 반환하고, `12`, `80`은 HTTP 200을 반환한다 |
| **실제 결과** | _(미실행 — 사전 테스트 명세)_ |

| 항목 | 내용 |
|------|------|
| **ID** | TC-015 |
| **시나리오** | AI 응답의 `text` 필드에 HEX 색상 코드가 포함되지 않는지 확인 |
| **사전 조건** | `/api/generate/section` 엔드포인트 접근 가능. AI가 응답할 수 있는 환경 |
| **테스트 스텝** | 1. `POST /api/generate/section`으로 생성 요청을 한다<br>2. 응답 JSON의 `sections` 배열을 순회한다<br>3. 각 섹션의 `items` 내 `text` 필드 값을 확인한다<br>4. `#[0-9A-Fa-f]{6}` 패턴 포함 여부를 검사한다 |
| **기대 결과** | 모든 `text` 필드 값이 HEX 색상 코드(`#FF0000` 등) 패턴을 포함하지 않는다 |
| **실제 결과** | _(미실행 — 사전 테스트 명세)_ |

| 항목 | 내용 |
|------|------|
| **ID** | TC-016 |
| **시나리오** | 이미지 생성 최종 프롬프트에 오버레이 전용 텍스트 지침이 포함되지 않는지 확인 |
| **사전 조건** | `/api/generate/section` 엔드포인트 접근 가능. 오버레이 텍스트가 포함된 페이로드 준비 |
| **테스트 스텝** | 1. 오버레이 텍스트가 포함된 페이로드로 `POST /api/generate/section`을 요청한다<br>2. 응답의 `finalImagePrompt` 필드를 확인한다<br>3. `"overlay"`, `"텍스트 디자인"` 키워드 포함 여부를 검사한다 |
| **기대 결과** | `finalImagePrompt`에 `"overlay"`, `"텍스트 디자인"` 키워드가 포함되지 않는다 |
| **실제 결과** | _(미실행 — 사전 테스트 명세)_ |

---

## 카테고리 5 — 인증 & 데이터 무결성

| 항목 | 내용 |
|------|------|
| **ID** | TC-017 |
| **시나리오** | 브랜드 프로필 CRUD 전체 사이클이 오류 없이 완료되는지 확인 |
| **사전 조건** | 인증된 사용자 세션 보유. 브랜드 프로필 생성·조회·삭제 권한 있음 |
| **테스트 스텝** | 1. `POST /api/brands`로 브랜드 프로필을 생성하고 상태 코드와 `id`를 확인한다<br>2. `GET /api/brands/{id}`로 조회하여 상태 코드를 확인한다<br>3. `DELETE /api/brands/{id}`로 삭제하여 상태 코드를 확인한다 |
| **기대 결과** | 생성은 HTTP 201, 조회는 HTTP 200, 삭제는 HTTP 200을 반환하며 각 단계에서 DB 오류가 없다 |
| **실제 결과** | _(미실행 — 사전 테스트 명세)_ |

| 항목 | 내용 |
|------|------|
| **ID** | TC-018 |
| **시나리오** | AI API 키 미설정 환경에서 생성 요청 시 500이 아닌 의미 있는 응답이 반환되는지 확인 |
| **사전 조건** | AI API 키가 설정되지 않은 환경(또는 mock 헤더 사용 가능). `/api/generate/detail-page` 접근 가능 |
| **테스트 스텝** | 1. `x-use-mock: true` 헤더와 함께 `POST /api/generate/detail-page`를 요청한다<br>2. HTTP 응답 상태 코드를 확인한다<br>3. 응답 본문에 `sections` 필드가 있는지 확인한다 |
| **기대 결과** | HTTP 500이 아닌 정상 응답이 반환되며 응답 본문에 `sections` 필드가 존재한다 |
| **실제 결과** | _(미실행 — 사전 테스트 명세)_ |

| 항목 | 내용 |
|------|------|
| **ID** | TC-019 |
| **시나리오** | 프로젝트 생성 전체 플로우(UI 입력 → API → DB → 리다이렉트)가 오류 없이 완료되는지 확인 |
| **사전 조건** | 사용자가 로그인 상태. `/dashboard/projects/new` 페이지 접근 가능. DB 연결 정상 |
| **테스트 스텝** | 1. `/dashboard/projects/new` 페이지에 접근한다<br>2. 제품명 "테스트 상품"을 입력하고 카테고리 "FASHION"을 선택한다<br>3. "프로젝트 생성" 버튼을 클릭한다<br>4. 리다이렉트 URL과 페이지 제목을 확인한다 |
| **기대 결과** | `/dashboard/projects/{id}` 형식의 URL로 리다이렉트되며 `h1`에 "테스트 상품"이 표시된다 |
| **실제 결과** | _(미실행 — 사전 테스트 명세)_ |

| 항목 | 내용 |
|------|------|
| **ID** | TC-020 |
| **시나리오** | 한글·특수문자 포함 커밋 메시지가 있는 브랜치에서 CI 파이프라인이 정상 통과되는지 확인 |
| **사전 조건** | GitHub Actions 워크플로우 설정 완료. 한글 커밋 메시지를 포함한 테스트 브랜치 존재 |
| **테스트 스텝** | 1. 한글 커밋 메시지(예: `feat: 한글 기능 추가`)가 포함된 브랜치를 push한다<br>2. GitHub Actions 워크플로우 실행 결과를 확인한다<br>3. 한글·이모지·특수문자 파일명이 포함된 경로의 빌드 결과를 확인한다 |
| **기대 결과** | CI 파이프라인이 한글 커밋 메시지·파일명으로 인한 인코딩 오류 없이 통과된다 |
| **실제 결과** | _(미실행 — 사전 테스트 명세)_ |

---

## 테스트 커버리지 요약

| 카테고리 | 케이스 수 | 주요 설계 기법 |
|----------|----------|---------------|
| 채팅 메시지 상태 관리 | TC-001 ~ TC-004 (4건) | 동등 분할, 상태 전이 테스트, 에러 추정 |
| 입력 유효성 검사 & API 계약 | TC-005 ~ TC-008 (4건) | 경계값 분석, 동등 분할, 에러 추정 |
| UI 상태 & 네비게이션 | TC-009 ~ TC-011 (3건) | 경계값 분석, 에러 추정, 파라미터화 테스트 |
| AI 생성 & 프롬프트 출력 | TC-012 ~ TC-016 (5건) | 에러 추정, 경계값 분석, 출력 분리 검증 |
| 인증 & 데이터 무결성 | TC-017 ~ TC-020 (4건) | CRUD 완전성, 에러 추정, 시나리오 기반 E2E |
| **합계** | **20건** | |
