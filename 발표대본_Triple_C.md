# Triple C 발표 대본

## 슬라이드 1: 표지

> 안녕하세요, 콘텐츠 1팀 Triple C 프로젝트를 발표하겠습니다.
> Triple C는 Contents, Copy, Creative의 약자로, AI 기반 마케팅 콘텐츠 자동 생성 에이전트입니다.
> 실제 운영 중인 코드를 기반으로 아키텍처와 개발 현황을 공유드리겠습니다.

---

## 슬라이드 2: 프로젝트 개요

> 저희 프로젝트의 핵심 목표는 상품 상세페이지 제작 시간을 1시간에서 10분 이내로 단축하는 것입니다.
> Gemini AI를 활용한 텍스트/이미지 생성과 RAG 기반 브랜드 분석이 핵심 기술이며, 올리브영 베스트셀러 398개 제품의 상세페이지 패턴을 학습시켰습니다.
> 타겟 사용자는 개인 크리에이터를 위한 B2C와 마케팅 팀을 위한 B2B 워크스페이스 두 가지 모델을 지원합니다.
> 단순 AI 생성이 아닌, 실제 한국 이커머스에서 검증된 마케팅 패턴을 데이터 기반으로 적용한 것이 차별점입니다.

---

## 슬라이드 3: 기술 스택

> 프론트엔드는 Next.js 14 App Router와 TypeScript, Tailwind CSS 및 shadcn/ui로 구성했습니다.
> 백엔드는 Next.js API Routes를 Serverless로 운영하며, Prisma ORM과 Neon PostgreSQL을 사용합니다.
> AI 텍스트 생성은 Gemini 2.0 Flash를 메인으로 사용하는데, 빠른 응답 속도와 한국어 처리 능력이 뛰어나기 때문입니다.
> 이미지 생성은 기본 gemini-2.5-flash-image와 Pro 버전인 gemini-3-pro-image-preview 두 가지 옵션을 제공합니다.

---

## 슬라이드 4: 시스템 아키텍처

> 전체 아키텍처는 클라이언트, 서버, AI 로직, 데이터 레이어 4계층으로 구성됩니다.
> 클라이언트 레이어에서 인증, 대시보드, 에디터, 마켓플레이스 등 19개 페이지를 제공하고, Vercel Edge Network를 통해 글로벌 CDN과 DDoS 방어를 처리합니다.
> 서버 레이어는 61개의 REST API 엔드포인트로 구성되어 있으며, /api/auth, /api/projects, /api/generate 등 도메인별로 분리했습니다.
> 외부 서비스로는 Neon PostgreSQL, Cloudinary 이미지 스토리지, 그리고 Google Gemini, OpenAI, Anthropic 등 멀티 AI 프로바이더를 연동했습니다.

---

## 슬라이드 5: 데이터 모델 (ERD)

> 데이터베이스는 총 26개 모델로 구성되어 있으며, 크게 4가지 도메인으로 분류됩니다.
> Auth & Org 도메인에는 User와 Workspace가 있어 B2C 개인 사용자와 B2B 팀 협업을 모두 지원합니다.
> Core Project 도메인의 Project 모델은 제품명, 카테고리, 브랜드 연결 정보를 담고 있고, BrandProfile은 톤앤매너와 이미지 키워드를 저장합니다.
> Generation Output 도메인의 DetailPageVersion은 AI가 생성한 훅 메시지와 섹션 데이터를 JSON으로 저장하며, 버전 관리가 가능합니다.

---

## 슬라이드 6: 주요 기능 1 - AI 상세페이지 생성

> 첫 번째 핵심 기능은 AI 상세페이지 자동 생성입니다. detail-page-generator.ts 파일 하나가 1,042줄로, 전체 생성 파이프라인을 담당합니다.
> MAIN, HERO, FEATURES, PROOF 등 마케팅에 최적화된 6가지 섹션을 자동으로 구성하고, RAG 시스템으로 브랜드 문서를 분석해 톤앤매너를 유지합니다.
> 특히 Contextual Image Gen 기능은 제품 이미지의 배경을 제거한 뒤, 섹션 문맥에 맞는 새로운 배경을 합성합니다.
> SHORT는 8장, MEDIUM은 12장, LONG은 15장으로 동적 길이 조절이 가능하며, 한 번의 입력으로 기능 중심과 감성 중심 두 가지 버전을 동시에 생성합니다.

---

## 슬라이드 7: 주요 기능 2 - 프롬프트 엔지니어링

> 두 번째 핵심 기능은 한국형 이커머스에 최적화된 프롬프트 엔지니어링 시스템입니다.
> 올리브영 베스트셀러 398개 제품, 12,470장의 이미지를 분석하여 패턴을 학습시켰습니다.
> 시스템 프롬프트에서 "한국 이커머스 상세페이지 전문 마케팅 카피라이터" 역할을 부여하고, 이모지 사용 금지 등 전문성을 확보했습니다.
> 스킨케어, 메이크업, 건강식품 등 30개 이상 카테고리별 소구점을 자동 최적화하고, "시카 크림" 입력 시 병풀잎, 물방울 같은 오브제를 자동 배치합니다.

---

## 슬라이드 8: 주요 기능 3 - 이미지 오버레이 에디터

> 세 번째 핵심 기능은 WYSIWYG 기반의 실시간 편집 도구입니다. detail-page-editor.tsx가 1,193줄로 구현되어 있습니다.
> 이미지 위에 Headline, Sub, Body 텍스트 레이어를 드래그 앤 드롭으로 자유롭게 배치할 수 있습니다.
> 가장 차별화된 기능은 AI 채팅 편집인데요, "배경을 좀 더 밝게 해줘"라고 자연어로 요청하면 AI가 즉시 반영합니다.
> 30초마다 자동 저장되고, 데스크톱/태블릿/모바일 뷰포트 전환으로 반응형 확인이 가능하며, HTML/JSON 포맷으로 원클릭 내보내기를 지원합니다.

---

## 슬라이드 9: 사용자 흐름

> 사용자 흐름은 7단계로 구성됩니다. 먼저 NextAuth.js 기반 회원가입과 이메일 인증을 거칩니다.
> 브랜드 프로필 단계에서 웹사이트나 SNS URL을 입력하면 RAG 시스템이 자동으로 크롤링하고 브랜드 특성을 분석합니다.
> 프로젝트 생성 후 제품 정보를 입력하면, AI가 10~30초 내에 2개 버전의 상세페이지를 생성합니다.
> 생성된 결과물을 에디터에서 수정하고, 최종적으로 HTML이나 JSON으로 내보내기하여 실제 쇼핑몰에 적용할 수 있습니다.

---

## 슬라이드 10: 데이터 흐름

> 데이터 파이프라인은 API 요청부터 결과 반환까지 8단계로 처리됩니다.
> Preparation 단계에서 제품 정보 입력, Zod 스키마 검증, 브랜드 RAG 컨텍스트 조회, 프롬프트 빌딩을 순차적으로 수행합니다.
> AI Engine 단계가 핵심인데, 이미지 분석(Vision), 텍스트 생성(LLM), 이미지 생성(I2I)을 병렬로 처리하여 속도를 최적화했습니다.
> Completion 단계에서 2개 버전을 조합하고, DB 저장과 크레딧 차감을 트랜잭션으로 처리한 뒤 에디터로 결과를 전달합니다.

---

## 슬라이드 11: 기획 상황 공유 (Sprint Board)

> 현재 전체 진행률은 58%이며, Sprint 1은 완료, Sprint 2는 진행 중, Sprint 3은 계획 단계입니다.
> Sprint 1에서는 NextAuth 인증, 프로젝트 CRUD, AI 생성 파이프라인, 에디터 v1 등 핵심 기능을 모두 완료했습니다.
> Sprint 2에서는 마켓플레이스 템플릿 시스템을 현재 구축 중이고, 올리브영 데이터 기반 400개 이상 템플릿을 업로드했습니다. Motion/GIF는 DB 스키마 설계가 완료되어 로직 구현 중입니다.
> Sprint 3에는 Runway API 연동한 숏폼 비디오 생성, WebSocket 실시간 진행률 표시, HD 이미지 업스케일링 등이 계획되어 있습니다.

---

## 슬라이드 12: 개발 상황 공유

> 현재까지 구현된 시스템 규모를 공유드립니다. 오늘 기준으로 커밋이 계속 진행되고 있습니다.
> 백엔드 API는 61개 엔드포인트, 프론트엔드는 19개 페이지, UI 컴포넌트는 77개를 구현했습니다.
> 데이터베이스는 User, Project, BrandProfile, DetailPageVersion 등 26개 핵심 테이블로 구성되어 있습니다.
> 전체 코드량은 4,000줄 이상이며, Generator 45%, API 25%, Utils 20%, Types 10% 비율로 구성되어 있습니다.

---

## 슬라이드 13: 구현 vs 계획 비교

> 구현 완료된 기능은 9개, 향후 계획된 기능은 8개입니다.
> 완료된 핵심 기능은 AI 텍스트/이미지 생성, 프로젝트/브랜드 CRUD, RAG 브랜드 분석, 이미지 오버레이 에디터, 버전 관리, 템플릿 시스템입니다.
> 향후 로드맵으로는 Motion/GIF 실제 생성, Runway 비디오 연동, Stripe 결제 완성, WebSocket 실시간 진행률, HD 이미지 옵션, 다국어 지원이 있습니다.
> 현재 MVP 수준의 핵심 기능은 모두 동작하며, 실제 서비스 배포가 가능한 상태입니다.

---

## 슬라이드 14: Thank You & Q&A

> 이번 프로젝트를 통해 AI 에이전트 개발의 전체 사이클을 경험할 수 있었습니다.
> 특히 프롬프트 엔지니어링에서 한국어 마케팅 카피의 특성을 반영하는 것이 예상보다 까다로웠고, 올리브영 데이터 분석을 통해 실제 패턴을 학습시키는 접근이 효과적이었습니다.
> 또한 Next.js App Router와 Vercel Serverless 환경에서 AI 파이프라인을 구현하며 타임아웃, 병렬 처리 등 실무적인 이슈들을 해결할 수 있었습니다.
> 질문 있으시면 말씀해 주세요. 감사합니다.

---

*작성일: 2025년 1월*
*프로젝트: Triple C - AI 마케팅 콘텐츠 에이전트*
